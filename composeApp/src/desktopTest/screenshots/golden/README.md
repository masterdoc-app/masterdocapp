# Golden UI screenshots (Desktop tests)

Generated by `FrequentIssuesScreenTest` via `captureToImage()`.

| File | Состояние |
|------|-----------|
| `frequent_issues_empty.png` | Станция выбрана, `GET /v1/report` вернул пустой список |
| `frequent_issues_list.png` | Две сохранённые неисправности (mock repository) |

Обновить снимки:

```bash
./gradlew :composeApp:desktopTest --tests FrequentIssuesScreenTest
```
