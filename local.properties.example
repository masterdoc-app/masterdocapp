# Copy to local.properties in the project root (local.properties is gitignored).
# Values are baked into all KMP targets at compile time (Android, iOS, Desktop, Web).
#
# Onyx API base URL must include the /api prefix:
onyx.baseUrl=http://80.87.196.33:3000/api
#
# Personal access token: Onyx → Settings → Accounts & Access → Create New Token
onyx.pat=
